# Git shortcuts
alias gs="git status"
alias gc="git commit"
alias gg="git log"

# GPG
alias gpg="gpg2"

# Sublime
alias s="subl"

# Local dotfiles
alias rcupl="rcup -d ~/code/eebs/dotfiles -x README.md"

alias trust="mkdir -p .git/safe"
